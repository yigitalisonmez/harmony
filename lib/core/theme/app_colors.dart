import 'package:flutter/material.dart';

abstract final class AppColors {
  static const background = Color(0xFF000000);
  static const foreground = Color(0xFFFFFFFF);
  static const primary = Color(0xFFFF7070);
  static const accent = Color(0xFFFFB3C6);
  static const card = Color(0xFF121212);
  static const secondary = Color(0xFF1A1A1A);
  static const border = Color(0xFF1A1A1A);
  static const muted = Color(0xFF1A1A1A);
  static const mutedForeground = Color(0xFF888888);

  // Semantic
  static const heartColor = primary;
  static const dismissColor = Color(0xFFFFFFFF);
}
